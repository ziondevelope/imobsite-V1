# Ambiente
NODE_ENV=production

# Porta do servidor (opcional, por padrão é 5000)
PORT=5000

# Configuração de sessão (opcional)
SESSION_SECRET=your_session_secret_here

# Configurações para o frontend (opcional)
# Todas as variáveis de ambiente do frontend devem começar com VITE_
VITE_API_URL=http://localhost:5000